package com.stsporting.core;

/**
 * App settings. Container only for now; values are not yet persisted
 * (persistence is optional / future work per the foundation spec).
 */
public class Settings {
    public float masterVolume = 1.0f;
    public boolean fullscreen = false;
}
