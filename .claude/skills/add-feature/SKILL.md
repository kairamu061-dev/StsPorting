---
name: add-feature
description: Add a feature area or sub-item to docs/. Use when creating a new feature area or sub-item directory.
argument-hint: <path> (e.g. auth or auth/login)
allowed-tools: Bash
---

Run the following command:

```bash
bash "${CLAUDE_SKILL_DIR}/add-feature.sh" $ARGUMENTS
```

Report the result briefly.
