{
  "name": "Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "claudeuser",
  "mounts": [
    "source=${localEnv:USERPROFILE}/.claude,target=/home/claudeuser/.claude,type=bind"
  ]
}
